Getting started with the Webdav driver
======================================

All the defaults variables are optional:

protocol: "http"
username: ""
password: ""
port: 8008
changes_timer: 60

An example of setup.json which use a private key (uses the default value) with an empty password (default value), a default port.

{
  "name": "example",

  "entries": {
    "LOC": {
      "driver": "local_storage",
      "options": {
        "root": "example/loc"
      }
    },
    "WEBDAV": {
      "driver": "webdav",
      "options": {
        "root": "/onitu",
        "hostname": "localhost",
        "changes_timer": 2,
        "protocol": "http"
      }
    }
  },

  "rules": [
    {
      "match": {"path": "/"},
      "sync": ["LOC", "ONITU"]
    }
  ]

}
