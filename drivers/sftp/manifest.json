{
  "name": "SFTP",
  "description": "Store files on a remote machine using SFTP.",
  "options": {
    "hostname": {
      "name": "Hostname",
      "description": "The hostname for the SFTP connection",
      "type": "string"
    },
    "username": {
      "name": "Username",
      "description": "The username for the SFTP connection",
      "type": "string"
    },
    "password": {
      "name": "Password",
      "description": "The password for the SFTP connection. You must use either this or a private key.",
      "type": "string",
      "default": ""
    },
    "port": {
      "name": "Port number",
      "description": "The port number for the SFTP connection",
      "type": "integer",
      "default": 22
    },
    "changes_timer": {
      "name": "Timer for modification check",
      "description": "Time in seconds between two checks for files modifications",
      "type": "integer",
      "default": 60
    },
    "private_key_path": {
      "name": "Private key path",
      "description": "The path to your private key file. You must use either this or a password.",
      "type": "string",
      "default": ""
    },
    "private_key_passphrase": {
      "name": "Private key passphrase",
      "description": "The passphrase for your private key (if using one)",
      "type": "string",
      "default": ""
    }
  }
}
