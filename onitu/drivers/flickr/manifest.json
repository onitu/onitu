{
  "name": "Flickr",
  "description": "Driver to synchronize files with a Flickr account.",
  "options": {

    "root": {
      "name": "Root",
      "description": "The root of the directories under Onitu's watch",
      "type": "string",
      "default": ""
    },

    "oauth_token": {
      "name": "Oauth token",
      "description": "Your oauth token retrieved with get_token.py",
      "type": "string"
    },

    "oauth_token_secret": {
      "name": "Oauth token secret",
      "description": "Your oauth token secret retrieved with get_token.py",
      "type": "string"
    }
  }
}