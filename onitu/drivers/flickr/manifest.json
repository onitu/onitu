{
  "name": "Flickr",
  "description": "A Driver to synchronize your photos and videos with a Flickr account. This driver works only from onitu to Flickr (If you add some photos via your flickr web interface, they not be downloaded on onitu)",
  "options": {

    "root": {
      "name": "Root",
      "description": "The root of the directories under Onitu's watch",
      "type": "string",
      "default": ""
    },

    "oauth_token": {
      "name": "Oauth token",
      "description": "Your oauth token retrieved with get_token.py",
      "type": "string"
    },

    "oauth_token_secret": {
      "name": "Oauth token secret",
      "description": "Your oauth token secret retrieved with get_token.py",
      "type": "string"
    }
  }
}