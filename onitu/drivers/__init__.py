"""
This module holds all the available Drivers. A new driver can be
creating by instanciating a new `Plug`, defining handlers via the
`Plug.handler` decorator, and sending the updates to the Referee via
the `Plug.update_file` method.
"""
