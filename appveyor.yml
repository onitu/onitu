version: '{build}'

environment:
  matrix:
    - PYTHON: "C:/Python27"
    - PYTHON: "C:/Python34"

  global:
    BOOST_ROOT: "C:/Libraries/boost"

install:
  - "git clone https://github.com/bureau14/leveldb.git"
  - "cd leveldb/"
  - "cmake ."
  - "msbuild ."
  - ps: "Get-ChildItem . -recurse"

build: off
deploy: off

test_script:
  - "%PYTHON%/Scripts/py.test.exe -v tests/"
